query GetSensorMeasurements($siteId: String!, $deviceId: String!, $sensorKey: String!, $range: TimeRange!) {
  getSensorMeasurements(siteId: $siteId, deviceId: $deviceId, sensorKey: $sensorKey, range: $range) {
    id
    sensor
    value
    takenAt
  }
}
