# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type UserModel {
  id: String!
  email: String!
  name: String!
  role: Role!
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum Role {
  ADMIN
  SITE_MANAGER
  SITE_VIEWER
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type AuthPayload {
  ok: Boolean!
  user: UserModel!
}

type AlertModel {
  id: String!
  siteId: String!
  deviceId: String
  type: String!
  severity: AlertSeverity!
  status: AlertStatus!
  message: String!
  lastNotifiedAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum AlertSeverity {
  WARNING
  CRITICAL
}

enum AlertStatus {
  ACTIVE
  RESOLVED
}

type SiteModel {
  id: String!
  name: String!
  role: Role!
  status: SiteStatus!
  lastUpdate: DateTime
}

enum SiteStatus {
  UNKNOWN
  OK
  WARNING
  CRITICAL
}

type MeasurementModel {
  id: String!
  sensor: String!
  value: Float!
  takenAt: DateTime!
}

type Query {
  getMe: UserModel!
  getSites: [SiteModel!]!
  getSite(id: String!): SiteModel!
  getMeasurements(siteId: String!, range: TimeRange!): [MeasurementModel!]!
  getSensorMeasurements(siteId: String!, sensorKey: String!, range: TimeRange!): [MeasurementModel!]!
  getAlerts(siteId: String, type: String, status: AlertStatus): [AlertModel!]!
  adminUsers: [UserModel!]!
}

enum TimeRange {
  LAST_24H
  LAST_7D
  LAST_30D
}

type Mutation {
  login(input: LoginInput!): AuthPayload!
  logout: Boolean!
  resolveAlert(id: String!): Boolean!
}

input LoginInput {
  email: String!
  password: String!
}